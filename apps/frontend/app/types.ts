export type Proposal = {
  block_number: string;
  calldata: string;
  description: string;
  proposalId: string;
  proposer: string;
  targets: string;
  timestamp_s: string;
  values: string;
  voteEnd: string;
  voteStart: string;
}
