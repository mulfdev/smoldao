export type Proposal = {
    block_number: string;
    calldata: string;
    description: string;
    proposalId: string;
    proposer: string;
    targets: string;
    timestamp_: string;
    values: string;
    voteEnd: string;
    voteStart: string;
    id: string;
};
